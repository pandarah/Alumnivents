//- This class creates the navigation bar at the top of the page. It has 3 buttons, of which
//- Login and Logout are dependent on the status of loggedIn
div(class='ui grid center aligned padded' id='heading')
    div(class='four wide column')
        if loggedIn
            button(class='ui button inverted' id='logout') Log Out
        else
            button(class='ui button inverted' id='login') Log In
    div(class='eight wide column')
        h1= name
    div(class='four wide column')
        button(class='ui button inverted' id='create') Create Event
include modals/Login
include modals/Create